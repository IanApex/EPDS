feat: add SRP filters organism with price/payment filter

- Add SrpFiltersPanel organism: sliding parent/child panel animation,
  body-style filter with vehicle icons, returning/selected state management
- Add SrpFilterPill component: location (blue) and dismissible variants
- Add SrpFilterChild component: bottom-border-only rows, returning state
  with green background and white separators, icon slot
- Add SrpPricePaymentFilter component: Price/Payment SegmentedControl,
  100px Min/Max inputs with clamp-on-blur, dual-thumb BaseSlider (1k/50
  step), commit event on pointer-release so pills only update on drag end
- Add SrpPage: desktop sticky sidebar with scroll, mobile drawer (Teleport,
  body scroll lock, full-width <=624px), active filter pills for body style,
  price range, and monthly payment; reset on dismiss
- Update SrpTile, CarouselDots, Tooltip, tokens, and design-system rules
